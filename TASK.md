为离线静态程序分析工程实现“整数循环的区间抽象解释”可复用模块。

输入小CFG，变量为数学整数，语句赋常量、复制、加常量及条件x<=c、x>=c；最多20变量、40块。实现区间域含无穷端点，合并取凸包；循环头第三次扩张开始标准widening，稳定后最多8轮narrowing。给每块入出不变量及是否证明assert范围；无法证明输出unknown，不判为错误。提供独立局部转移包含性检查器，禁止用有限采样冒充证明。

验收与演示：小有界程序具体执行对照覆盖性；测试无界增长、不可达分支、收窄改善和两个回边；demo展示proved与unknown。

使用 Python 3.14.7 标准库，从零交付可导入的本题库，不调用第三方同类实现代替核心算法。Linux 原生运行，安装 Python 后无需安装其他依赖；核心、测试、演示全部离线，不需要账号、真实密钥、网络、硬件、独立数据库、中间件、云 API 或 Docker。示例数据用仓库内的确定性代码生成，不依赖未提供附件、在线语料或其他题目代码。

先固定公开数据结构、接口及错误语义：数值输入拒绝 NaN/Infinity，整数参数拒绝 bool；本文有更具体契约时以具体契约为准。非法输入给可定位的错误，不静默纠正。批次/有状态操作失败时不能留下未说明的部分变更；算法搜索预算耗尽与证明无解必须分开。浮点结果在 README 明确误差口径，不能用宽容差掩盖边界错误；有限协议/数学子集只按本题契约实现，不宣称完整行业标准兼容。

只交付本题模块、必要测试、固定输入 demo.py 与 README。README 写明 Python 版本、无需额外依赖、接口、输入上限、设计取舍、已知限制和准确命令。用 python -m unittest discover -s tests -v 执行必要测试；独立参考不得直接调用被测核心得到期望值，往返一致仅能作为补充。用 python demo.py 在约8秒内真实计算并打印一个正常结果和一个实际触发的拒绝/异常边界，不用 sleep 或预录结果，不做长时间压力测试。题面给出的上限是接口边界，短演示不必穷举上限规模。

一次首轮完成实现及必要验证，不追问、不要求回复“继续”。不扩展成网站、CLI 产品、报表看板、常见游戏、文件管理器或业务 CRUD 平台；终端命令仅用于启动测试和演示。只操作当前工作区，不读取用户配置、凭据、环境变量清单、保密脚本或其他工作区。不添加 Actions、CI/CD、自动部署、定时任务或 Dependabot，不向远端 push。最后如实报告通过、失败和未执行的验证，不把正常结束当作功能全部正确。
